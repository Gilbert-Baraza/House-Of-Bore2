# requirements/base.txt
# Packages required in ALL environments (development and production).
# Install with: pip install -r requirements/base.txt

Django>=5.1

# Reads .env files and maps variables to Python settings cleanly.
# Supports casting (booleans, lists, ints) — avoids manual os.environ.get() calls.
python-decouple>=3.8

# Asynchronous task queue and broker clients for event-driven notifications
celery>=5.4
redis>=5.0

# django-redis: production-grade Redis cache backend for Django.
# Provides connection pooling, compression, Sentinel support, and key pattern deletion.
django-redis>=5.4

# hiredis: high-performance C parser for Redis responses.
hiredis>=3.0
