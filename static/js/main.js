/**
 * static/js/main.js
 * ─────────────────────────────────────────────────────────────────────────────
 * House of Bore — Global JavaScript
 *
 * This file serves as the main entry point for site-wide client-side logic.
 * In Phase 1.4, we keep JavaScript minimal and rely on CSS-first patterns
 * (such as the checkbox peer trick for the mobile navbar and inline handlers
 * for dismissible alerts and modals).
 *
 * FUTURE ENHANCEMENTS (Phase 2+):
 *   - Alpine.js initialization and global stores (e.g., cart drawer, modals)
 *   - AJAX form submissions and dynamic cart updates
 *   - Accessibility enhancements (focus trapping in modals, ESC key listeners)
 * ─────────────────────────────────────────────────────────────────────────────
 */

document.addEventListener('DOMContentLoaded', () => {
  // Global event listeners and initializations will be registered here.
  console.debug('House of Bore UI foundation loaded.');
});
